export interface EnabledActions {
  actions: boolean;
  clone: boolean;
  create: boolean;
  edit: boolean;
  flag: boolean;
  folderChange: boolean;
  revert: boolean;
  save: boolean;
  spreadsheetIngest: boolean;
  upload: boolean;
}
