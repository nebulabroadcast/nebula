import { NavLink } from 'react-router-dom'
import { Navbar, InputText, Button, Spacer } from '/src/components'

const SchedulerNav = ({}) => {
  return (
    <Navbar>
      <Button icon="chevron_left" onClick={() => {}} />
      <Button icon="chevron_right" onClick={() => {}} />
    </Navbar>
  )
}

export default SchedulerNav
